# Integrated Linux T12 checkpoint

Source `ad577ab17427b0bcc08f7ebf7d75b22db157c087` ran the actual write group in Debug: **65/65 tests passed**, exit0, clean unchanged source. `run.json` preserves exact command, environment overrides, compiler/Python/driver/source/binary/log identities. This supplements both-mode worker proof at `1f12553`; it is not a new ReleaseSafe or production-host result.

The capture executed in the same recorded driver retained26 exec-fixture directories and222 raw records, with archive/member hashes and target/sentinel digests. At this source, the collector retained child/case/state records but omitted the new `history.json` file. Follow-up `f8e174a` adds that named history record for later native runs; no source or runtime result here is relabeled as that follow-up. Existing worker evidence contains the complete history records.

Full native GitHub driver execution, Mac recovery, genuine kernel faults, production supervisor wiring and power-loss gates remain open. Production writes remain disabled.
