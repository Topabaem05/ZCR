T11 checkpoint source 9a8473ae0eed17b0d80dc5b5044ff7990ce43e72 implements exact UTF-8 single-file patch and atomic Linux no-replace create under the approved publication guard. Production writes remain disabled.

Debug and ReleaseSafe each pass 25/25 write tests with fault injection, 54/54 isolation, 11/11 traversal, 14/14 read/deadline, and direct 12/12 memory tests. Contract verification passes. A non-test ReleaseSafe executable asserts the runtime fault hook is void and both I10/I11 immediately return Unsupported. All tracked allocation lifetimes drain and descriptor-owned temps are cleaned on tested failures.

The full Debug build attempted all registered tasks: 192/192 executable tests passed, but command exit is 1 because T08 and T13 registered sources are absent in this prepared checkout. The memory build group separately also exits 1 due absent T13, so T03 was compiled with its exact modules and run from the isolated cache in both modes. These failed builds are not reported as suite PASS.

Native Linux tests execute renameat2(NOREPLACE), renameat replacement, file/directory fsync, real hardlinks and FIFO, user xattr and POSIX ACL safe refusal, ordinary mode preservation, and concurrent publishers. The ACL fixture uses the mapped owner UID; an earlier unmapped UID fixture returned EINVAL and is retained as historical setup failure. ENOSPC is injected through the real Io write vtable; read-only and metadata-copy failures use explicit Editor hooks. Actual exhausted filesystem and read-only mount tests are NOT_RUN. macOS/Windows runtime, real subprocess crash/restart, power-loss and durable journal tests are NOT_RUN and belong to T12/platform gates. This does not claim hostile same-UID external CAS.

The in-memory JournalStore is bound to security domain/workspace incarnation/task and compares canonical payload digests. It is test-only; it is not crash recovery. A PREPARED failure/abort remains pending until the future durable journal transition implementation. I19 metadata/transition and atomic failRecovery guard supplements are root-owned follow-up interfaces.

Independent review found duplicate zero-width insertion positions remain accepted in this checkpoint, contrary to docs08 section4. That is a blocking follow-up, to receive an actual RED regression before correction. Reviewer also retains the already-reported record-failure disable race: releasing the guard before disabling writes admits a waiting publisher; disable failure currently fail-stops. Root is providing guard.failRecovery to eliminate this window. This checkpoint is historical verification of the reviewed source, not task completion or production approval.

Owned source: src/fs/edit.zig, src/fs/publish.zig, tests/t11_test.zig. Frozen I10/I11 conformance checks remain. Editor.Options supplies request allocator/reservation, registry, authorizer, session, bound journal and Cancel. Borrowed inputs/context must outlive the synchronous call; receipt idempotency key borrows the request or journal storage. required_fds=8; input+output buffers stay within the request reservation; file output cap=8 MiB, original UTF-8 spans<=1024.

Authorization base98c722e remains unchanged. Coordinator dependency commits ec54829/0982152/ba47459 were delivered by the root, corresponding to approved ab5df13/02a4ee4/f8a3b24. Source/config/corpus hashes, exact commands/exits, final binaries, standard clean-source record/verify results, and complete raw logs are attached. All caches, logs, binaries and temporary files were generated in state/T11 outside source.
