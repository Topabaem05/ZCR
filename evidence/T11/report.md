T11’s Linux atomic patch/create implementation is complete and source-approved at `09422d47fcf98d95f05a36d00c304846d3e1e6f8`. Production write entry points remain hard-disabled outside test compilation. T12’s persistent journal, real crash/restart proof and production integration remain separate gates; macOS and Windows publication backends/runtime proof are not delivered by this Linux task.

Final validation used Zig 0.16.0, this worktree, and task-local caches/TMP/output directories under `state/T11`. Every final run exited 0. The exact commands, source commit, logs, binary hashes and configuration/corpus digests are in `evidence/T11/final/runs.json`; standard clean-source `record` and `verify` attest both write binaries.

| Executed group | Debug | ReleaseSafe |
| --- | ---: | ---: |
| T11 write, explicit fault injection | 41/41 | 41/41 |
| T02/T10 isolation and publication coordinator | 56/56 | 56/56 |
| T05 traversal dependency | 11/11 | 11/11 |
| T04 read and deadline dependency | 12/12 | 12/12 |
| T03 memory, direct compile/run with exact module graph | 12/12 | 12/12 |

Contract verification passed through the build target and through the directly hashed `zcr-dev-evidence` executable. A non-test ReleaseSafe executable ran both I10/I11 and confirmed `Unsupported` before any authority/IO object is used. Compile-time checks prove `production_writes_enabled=false`, `Editor.fault`, `Temp.probe` and `Parent.after_publish_error` have void type in that executable. Runtime test-hook enablement is not available.

The earlier full Debug build executed 192/192 available tests but exited 1 because the prepared checkout registers absent T08/T13 sources. Its memory group also exits 1 for absent T13, so T03 was compiled and run separately. Those blocked builds are preserved as historical failures and are not final suite PASS. Correction to checkpoint prose and earlier status messages: the IO dependency count was **12/12**, not 14/14; its raw records/logs were always correct. Root integration has additional tests and may report different totals.

| Task scenario | Implemented evidence |
| --- | --- |
| WR-001 | Whole SHA-256, two original spans, unchanged CRLF/LF bytes, ordinary mode, receipt generation, exact publication IDs and durable create stage sequence. |
| WR-002 | Stale digest refuses; an actual mediated replacement while the outer request retains its original descriptor is detected at final validation and returns VersionConflict without overwriting the winner. |
| WR-003 | Sorted non-overlap, overflow/end bounds, UTF-8 boundaries/NUL, duplicate insertion positions, 8 MiB input/output bounds, key restrictions and unsupported durability. |
| WR-004 | Precommit cancel/deadline leaves old bytes; postcommit cancel keeps applied receipt; revoke-before-admission refuses; concurrent revoke waits for the admitted filesystem commit. |
| WR-007 | Every request allocation failure, real Io ENOSPC boundary injection, explicit read-only/metadata/prepare/publish/sync/record faults, real xattr/ACL refusal, temp collisions/replacement, ambiguous syscall success reconciliation, journal transition/errors, and queued publication blocked by recovery/durability failure. |
| WR-009/010 | Existing create never overwrites; kernel RENAME_NOREPLACE race has one complete winner; concurrent Editor callbacks have one winner; distinct task-bound sessions are excluded by Busy at writer lease admission. |
| IS-007 | Hardlink/FIFO refusal and moved-parent detection. Unprovable moved-parent cleanup now returns RecoveryRequired, preserves pending evidence and quarantines. |

The source uses same-parent random O_EXCL temps, nofollow/nonblocking descriptor-relative opens and identity checks. Only regular single-link files with ordinary supported mode and matching UID/GID are replaced. Linux ACLs, xattrs, special mode bits and unsupported attributes are refused before replacement. Actual POSIX ACL and user-xattr fixtures ran successfully; the initial unmapped-UID ACL setup failure is preserved as historical, not a platform failure. Native Linux renameat/renameat2, file fsync and directory fsync executed. ENOSPC was injected at the actual Io write vtable boundary; a physically exhausted filesystem and an actual read-only mount are NOT_RUN. No hostile same-UID external CAS, power-loss guarantee or real durable journal is claimed.

I10/I11 signatures remain conformant. `Editor.init(Options)` binds a request allocator/reservation, immutable session, registry, authorizer, journal context and Cancel. The allocator must draw from that reservation; borrowed input/context must remain valid and unchanged for the synchronous call. Returned idempotency text borrows request or journal storage. The editor requires eight reserved FDs and bounds old-plus-new allocation within the request grant. All tested allocation lifetimes and callbacks drain before their fixtures are destroyed.

The native I19 supplement is root-delivered. PREPARED receives the live workspace/root/parent/temp/original IDs, exact generated temp basename, lease fence, requested durability and a preallocated receipt UUID. The store must copy borrowed metadata and bind the full `(security_domain, workspace_incarnation, task, key)` context; receipt-only calls cannot select a namespace from a bare key. APPLIED follows successful/reconciled rename and markApplied; ABORTED is emitted only after proven prepublication failure, parent validation and successful exact-temp cleanup. Ambiguous cleanup or preparation remains pending and preserves the temp instead of fabricating an abort.

The publication guard remains owned through failure resolution. Postcommit journal failures return an applied RecoveryRequired receipt and use `guard.failRecovery()` before any waiter can publish. A persisted E_DURABILITY receipt remains applied and recovery-aware; the guard also quarantines that workspace. Before-guard uncertainty retains a callback and uses allocation-free `Registry.failRecovery(id)`. Filesystem/integrity errors or impossible lookup/prepare tags quarantine as RecoveryRequired. Busy/resource/contract refusals are ordinary only under the explicit store contract that they are definitely side-effect-free admission refusals. Trusted `setManagedWrite(true)` is startup/recovery-only after successful exclusive recovery and all quarantine activity drains.

T12 can use the stable 12-event test-only Stage API: after_read, after_temp_chunk, before_metadata, after_temp, after_prepare, before_commit, guarded, after_commit, before_sync, after_sync, before_record, after_record. Chunk/metadata/sync/receipt events occur at the actual operations. These hooks are instrumentation, not crash proof. T12 must execute real spawned child kill/restart/recovery tests with the persistent store and explicit trusted continuity; optional transition omission is permitted only for fake stores under builtin.is_test.

RED history is preserved. Initial stub source48648ae produced 11 behavioral failures. Duplicate insertion REDa9d7091, cleanup quarantine RED5f02a30, final mediated-version REDae47c66 and journal/durability RED414b52e all executed before their fixes. The final journal RED observed a queued filesystem publisher actually succeeding after unresolved durability, plus three storage uncertainty failures. The earlier three transition assertions ran in working test edits based on c4be790, later committed with an additional cleanup test; that log is genuine preimplementation RED but is explicitly not presented as a clean-source binary attestation. No historical PASS substitutes for the final09422d4 runs.

Independent source and final evidence review approved09422d4 with no remaining findings; the reviewer rehashed all 17 current run records, source/configuration/corpus/binaries, standard attestations, three archives with 107 members and four commit-bound RED records. The review artifact is `state/reviews/t11-review.md`. Integration-binary validation belongs to the root. Owned changes are confined to edit.zig, publish.zig, tests/t11_test.zig and evidence/T11. `final/scope.json` distinguishes root-delivered coordinator/core commits from owned commits. Original manifest base98c722e and authorization are retained. Final source/config/corpus hashes, archives, standard evidence, failure logs and binary paths are attached; source has no generated caches or output artifacts.
